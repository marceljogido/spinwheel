# PostgreSQL connection string in standard URI format
DATABASE_URL=postgres://postgres:password@localhost:5432/spinwheel

# Optional fine-grained configuration if you prefer discrete variables
PGHOST=localhost
PGPORT=5432
PGDATABASE=spinwheel
PGUSER=postgres
PGPASSWORD=password

# Server configuration
PORT=4000
CLIENT_ORIGIN=http://localhost:5173
ENABLE_DB_SSL=false

SESSION_TTL_MS=28800000
